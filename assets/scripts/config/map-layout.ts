/**
 * 地图布局（v1.2.0）
 *
 * 坐标系：设计分辨率 1280x720，原点在画面正中心。
 *
 * ```text
 *        红方建造区(9列)   河   蓝方建造区(9列)
 *      x:-570 ~ -90      0     90 ~ 570
 *   y:+250 ┌───────────┐ ┃ ┌───────────┐
 *   y:+100 │  上区 4 行 │ ┃ │  上区 4 行 │   ← 河沿 y 轴贯穿全场
 *          ├───────────┤ ┃ ├───────────┤
 *          │  战斗道路  │ ═│═ 桥  道路  │   ← 道路沿 x 轴居中（y=0，高 140）
 *          ├───────────┤ ┃ ├───────────┤
 *   y:-100 │  下区 4 行 │ ┃ │  下区 4 行 │
 *   y:-250 └───────────┘ ┃ └───────────┘
 * ```
 *
 * 规则：
 * - 道路（含跨河的"桥"）是唯一通道：单位只有在道路走廊内（|y| ≤ roadHalfHeight）
 *   才允许跨越中线（x 变号）
 * - 建造网格行从 ±100 起，道路半高 70 → 道路与格子互不重叠
 * - 河宽 120（±60），网格最内列 ±90 → 河不占建造格
 */

export const MAP_LAYOUT = {
    /** 战斗道路中心 y（画面垂直正中） */
    roadCenterY: 0,
    /** 战斗道路半高（高 140px，与 ±100 的网格行保持 30px 间隙） */
    roadHalfHeight: 70,
    /** 河中心 x（画面水平正中，y 轴） */
    riverCenterX: 0,
    /** 河半宽（宽 120px，与 ±90 的最内列保持 30px 间隙） */
    riverHalfWidth: 60,
    /** 河在画面上的纵向延伸半长 */
    riverHalfLength: 320,
} as const;
